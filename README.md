Готово — переписал README **полностью с нуля**, сохранив смысл, структуру, стиль и соответствие **текущему состоянию плагина**.
Теперь он чище, логичнее построен, без повторов, технически точный, полностью синхронизирован с твоим кодом (без HTML UI).

---

# 📌 **README.md — Moss Stats Plugin (актуальная версия)**

```markdown
# 🌿 Moss Stats Plugin  
### Paper 1.21.6 · REST API для ванильной статистики игроков Minecraft

**Moss Stats Plugin** — это высокопроизводительный Paper-плагин, который читает *ванильные файлы статистики*  
(`stats/<uuid>.json>`), кэширует их в памяти и предоставляет удобный REST API для доступа к данным.

Плагин одинаково хорошо работает с **онлайн и оффлайн игроками**, автоматически обновляет их статистику и  
предоставляет расширенные возможности выборки через HTTP.

---

# ✨ Возможности

### ✔ Чтение нативной статистики Minecraft
Плагин считывает JSON-файлы статистики из мира:

```

<world>/stats/<uuid>.json

````

Поддерживаются все стандартные категории:

- `minecraft:custom`
- `minecraft:mined`
- `minecraft:crafted`
- `minecraft:used`
- `minecraft:broken`
- `minecraft:picked_up`
- `minecraft:dropped`

Включая поля:
- play_time  
- jumps  
- deaths  
- walk/swim/fly distance  
- blocks mined  
- items crafted  
- mob_kills / player_kills  

### ✔ Кэширование в память
Все файлы загружаются в `ConcurrentHashMap` — быстрый доступ для API.

### ✔ Предзагрузка оффлайн статистики
При старте сервера загружаются все stats-файлы из мира.

### ✔ Автообновление онлайн игроков
Обновляется каждые N секунд (параметр в конфиге).

### ✔ Web API (REST) на встроенном HTTP сервере
API поднимается автоматически, порт задаётся в конфиге.

### ✔ Online/offline статус
API резолвит реальный статус игрока через `Bukkit.getPlayer()`.

### ✔ Универсальная система топов
Любая статистика поддерживает сортировку топ-20.

---

# 🔧 Конфигурация `config.yml`

```yaml
# Интервал обновления статистики онлайн игроков (в секундах)
update-interval-seconds: 60

# Порт HTTP API
web-port: 8080

---

# 🚀 Установка

### 1. Сборка:

```
mvn clean package
```

### 2. Поместить JAR-файл в:

```
/server/plugins/
```

### 3. Запустить Paper:

```
java -jar paper.jar
```

---

# 🌐 REST API — документация

## 📘 Получить всех игроков

```
GET /moss/players
```

Возвращает:

* uuid
* name
* online/offline
* полную статистику игрока

---

## 📘 Получить игрока по UUID

```
GET /moss/players/<uuid>
```

---

## 📘 Получить игрока по имени

```
GET /moss/player/<name>
```

---

## 📘 Онлайн игроки

```
GET /moss/online
```

---

## 📘 Сводная статистика

```
GET /moss/summary
```

Пример:

```json
{
  "players": 51,
  "totals": {
    "total_jumps": 1524421,
    "total_deaths": 922,
    "total_playtime": 18399122,
    "blocks_mined": 55324421,
    "items_crafted": 233525
  }
}
```

---

# 🔥 Топы по статистике

### Предустановленный эндпоинт:

```
GET /moss/top/jumps
```

### Универсальный топ:

```
GET /moss/top/<stat_key>
```

Примеры:

```
/moss/top/minecraft:jump
/moss/top/minecraft:stone
/moss/top/minecraft:player_kills
```

Всегда возвращает топ-20 (или другое число согласно `top-limit`).

---

# 🏗 Архитектура

```
StatsPlugin.java      — точка входа, запуск API и обновлений
StatsManager.java     — загрузка и кэширование статистики
StatsUtil.java        — работа с JSON-файлами статистики
WebServer.java        — обработка REST-запросов
```

---

# 📦 Сборка проекта

```
mvn clean package
```

Готовый JAR:

```
target/moss-stats-*.jar
```

---

# ⚠ Требования

* Java 21
* Paper 1.21.6
* Maven 3.8+

---

# 🛠 Roadmap

* [ ] Авторизация по API-ключу
* [ ] Оптимизация summary (кэширование)
* [ ] Rate limiting
* [ ] Расширенная аналитика (K/D, активности, графики)
* [ ] Внешний UI (отдельный web-проект)

---

# 💬 Обратная связь

Не
